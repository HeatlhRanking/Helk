spring:
  datasource:
    url: jdbc:h2:tcp://localhost/C:/H2/dbFile/helk
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  h2:
    console:
      enabled: true
